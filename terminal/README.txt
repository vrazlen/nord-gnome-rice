Terminal config targets Ptyxis (detected via RPM).
Palette installation uses a .palette file placed in ~/.local/share/org.gnome.Ptyxis/palettes/ or via ptyxis --install-palette.
Opacity can be set with scripts/ptyxis-opacity.sh.
